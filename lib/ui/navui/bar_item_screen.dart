import 'package:flutter/material.dart';

class BarItemScreen extends StatelessWidget {
  const BarItemScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(child: Text("Bar Item Screen")),
      ),
    );
  }
}
